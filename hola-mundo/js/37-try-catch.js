'use strict'

try {
    var year = 2019;

    var vector = Array(10);
    //alert(year);
    console.log(decodeURIComponent("https://tmaq.mx"));
} catch (error) {
    alert("A ocurrido un error");
    console.log(error);
}

